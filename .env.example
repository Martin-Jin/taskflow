# All variables below are optional — anything left blank falls back to mock data.

# Todoist personal API token (Settings -> Integrations -> Developer)
VITE_TODOIST_API_TOKEN=

# Google Calendar OAuth client (Google Cloud Console -> Credentials). Also
# used for "Sign in with Google" itself (see AuthContext.jsx) — without this,
# sign-in doesn't work at all, not just Calendar sync.
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_API_KEY=

# Google Calendar persistent auth — URL of the companion Cloudflare Worker's
# calendar-auth routes (see cloudflare-worker/README.md to deploy/configure
# them). Required for Google Calendar sync to work at all: the client no
# longer holds the OAuth client secret needed to redeem an authorization
# code, so without this set, connecting to Google Calendar will fail.
VITE_CALENDAR_AUTH_WORKER_URL=

# AI Quick Add — URL of the companion Cloudflare Worker that proxies Claude/
# Gemini calls (see cloudflare-worker/README.md to deploy your own). Optional:
# leave blank and the "AI Quick Add" button simply doesn't appear.
VITE_AI_QUICKADD_WORKER_URL=

# Optional, local dev only: your own Anthropic/Gemini API key, so you don't
# have to paste it into Settings on every fresh `npm run dev`. Real users
# (including you on the deployed site) add their own key in Settings ->
# Integrations -> AI Quick Add instead — it's never baked into the build.
VITE_ANTHROPIC_API_KEY=
VITE_GEMINI_API_KEY=
